version: '3.9'

services:
  archiva:
    image: xetusoss/archiva:latest
    container_name: archiva
    restart: unless-stopped

    ports:
      - "8081:8080"
    
    environment:
      - JAVA_OPTS=-Xms512m -Xmx1024m

    volumes:
      - /opt/archiva/data:/archiva-data
      - /opt/archiva/logs:/archiva-logs
      - /opt/archiva/conf:/archiva-conf